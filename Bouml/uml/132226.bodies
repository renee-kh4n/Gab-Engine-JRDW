class ContactResolver
!!!133122.cpp!!!	ResolveContacts(in contacts : std::vector<CollisionContact*>, in time : float) : void

	int iterations = 0;
	while (iterations < this->max_iterations)
	{
		for (auto& contact : contacts)
		{
			contact->Resolve(time);
		}

		iterations++;
	}

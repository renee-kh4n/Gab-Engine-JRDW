#include "GUIPipeline.h"

gde::GUIPipeline::GUIPipeline(void* handle)
{
	//this->mWindow = new sf::RenderWindow(sf::VideoMode(800, 800), "SFML works!");
    //this->mWindow->setActive(false);
}

void gde::GUIPipeline::RenderFrame()
{
    
}
